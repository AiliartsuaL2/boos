spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
#    mapper-locations: classpath:/mapper/*.xml
    url: jdbc:mysql://database-1.cjwbmziqc77f.ap-northeast-2.rds.amazonaws.com:3306/boosdb?serverTimezone=Asia/Seoul
    username: root
    password: 1705914joo!
  jpa:
    hibernate:
      ddl-auto : update # DDL 정의시 DB의 고유 기능 사용 가능.
    show-sql: true # SQL 쿼리 확인
    properties:
      hibernate:
        format_sql: true #Hibernate가 SQL 가독성 높여줌


#swagger




# mybatis
#mybatis:
#  mapper-locations : classpath:mapper/*.xml